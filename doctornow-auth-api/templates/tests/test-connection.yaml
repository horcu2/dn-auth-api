apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "doctornow-auth-api.fullname" . }}-test-connection"
  labels:
    {{- include "doctornow-auth-api.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "doctornow-auth-api.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
