apiVersion: v1
kind: Service
metadata:
  name: {{ include "doctornow-auth-api.fullname" . }}
  labels:
    {{- include "doctornow-auth-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "doctornow-auth-api.selectorLabels" . | nindent 4 }}
